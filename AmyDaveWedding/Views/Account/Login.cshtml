@model AmyDaveWedding.Models.LoginViewModel

@{
    Layout = "~/Views/Account/_AccountLayout.cshtml";
    ViewBag.Title = "Log in";
}

@section css {
    <link href="/Content/bootstrap-social.css" rel="stylesheet" />
}

<h2>@ViewBag.Title</h2>
<div class="row">
    <div class="col-sm-5" >
        <section id="socialLoginForm">
            @Html.Partial("_ExternalLoginsListPartial", new { UserLoginProviderTypes = ViewBag.UserLoginProviderTypes, Action = "ExternalLogin", ReturnUrl = ViewBag.ReturnUrl })
            
            <p class="contact-us">
                If you have trouble logging in, please contact Dave or Amy.
            </p>
        </section>
    </div>
    @*<div class="col-sm-offset-1 col-sm-6">
        <hr class="visible-xs"/>
        <section id="loginForm">
            @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {
                @Html.AntiForgeryToken()
                <h4>Use a local account to log in.</h4>
                <p>
                    @Html.ActionLink("Register", "Register") if you don't have a local account.
                </p>
                <hr />
                @Html.ValidationSummary(true)
                <div class="form-group">
                    @Html.LabelFor(m => m.UserName, new { @class = "col-sm-3 control-label" })
                    <div class="col-sm-9">
                        @Html.TextBoxFor(m => m.UserName, new { @class = "form-control" })
                        @Html.ValidationMessageFor(m => m.UserName)
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.Password, new { @class = "col-sm-3 control-label" })
                    <div class="col-sm-9">
                        @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
                        @Html.ValidationMessageFor(m => m.Password)
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-offset-3 col-sm-9">
                        <input type="submit" value="Log in" class="btn btn-default login-button" />
                        <span class="checkbox remember-me">
                            @Html.CheckBoxFor(m => m.RememberMe)
                            @Html.LabelFor(m => m.RememberMe)
                        </span>
                    </div>
                </div>
            }
        </section>
    </div>*@
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}